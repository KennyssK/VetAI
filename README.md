# 🐾 VetAI | Clinical Decision Support System (CDSS)

**Official Version:** `0.4.1-alpha` | **Neural Kernel:** `v15_opt (TensorFlow-Powered/Optimized)`  
**Academic Partner:** [Rosbiotech University] | **Project Status:** University Startup

---

## 📋 Overview
**VetAI** — это высокотехнологичная экосистема для предиктивной аналитики в ветеринарии. Мы разрабатываем «умное ядро», которое обучается на миллионах синтезированных и реальных клинических случаев, чтобы обеспечить врачей точным диагностическим вектором.

Ядро **Neural Kernel v15_opt** использует гибридную архитектуру: сочетание глубоких нейронных сетей (**Deep Learning**) на базе **TensorFlow** и методов градиентного бустинга для интерпретации табличных данных.

---

## 👥 Core Team & Authorship

Проект VetAI является результатом синергии передовых технологий разработки программного обеспечения и глубокой доказательной ветеринарной медицины.

### **Романченко Ефим Денисович**
**Role:** Chief Technology Officer (CTO) & Lead System Architect, Full-Stack Engineer, ML Architect.
* **Contribution:** Проектирование и реализация полноцикловой архитектуры системы (Full-Stack Development). 
* **Engineering:** Разработка нейросетевого ядра v15_opt, создание кастомного препроцессора данных, проектирование микросервисной архитектуры (Backend/Frontend) и деплой-инфраструктуры.
* **Expertise:** Системная интеграция, оптимизация нейронных сетей под высоконагруженные задачи, архитектура Deep Learning моделей.

### **Свечников Илья Андреевич**
**Role:** Chief Medical Officer (CMO) & Domain Expert (DVM), Veterinary Expert, Medical Data Scientist.
* **Contribution:** Научное руководство и формирование фундаментальной базы знаний проекта. 
* **Data Science:** Сбор, верификация и структурирование клинических данных. Разработка уникальных алгоритмов медицинского парсинга на основе фундаментальной ветеринарной литературы.
* **Expertise:** Действующий ветеринарный врач. Эксперт в области дифференциальной диагностики и формирования датасетов для клинических экспертных систем.

---

## 📊 Dataset: Expert-Curated Medical Data

Одной из главных инноваций VetAI является его **Data-Origin**. В отличие от моделей, обученных на неструктурированных данных из открытых источников, наше ядро базируется на:

1. **Expert Synthesis:** База данных формировалась на протяжении длительного времени практикующими врачами.
2. **Clinical Accuracy:** Каждый паттерн в `DISEASE_PROFILES` прошел верификацию на соответствие протоколам доказательной медицины.
3. **Structured Knowledge:** Перевод многолетнего клинического опыта в машиночитаемые CSV-матрицы с учетом тончайших нюансов патофизиологии.

---

## 🛠 Technology Stack & Infrastructure
Проект упакован в высокопроизводительную среду для обеспечения стабильности и масштабируемости.

### Model Environment (Dockerized)
Мы используем специализированный стек для GPU-ускорения, что позволяет проводить инференс (предсказание) за миллисекунды.
* **Base Image:** `tensorflow/tensorflow:2.11.0-gpu`
* **Core Engine:** TensorFlow 2.11.0 + Keras
* **Explainability (XAI):** `SHAP` (SHapley Additive exPlanations) для интерпретации того, почему модель выбрала конкретный диагноз.
* **Deployment:** Flask + Gunicorn для API-слоя, Streamlit для фронтенд-демо.

---

## 🔬 Architecture: Neural Kernel v15
Архитектура v15 построена на принципе **Biologically Constrained ML** (Машинное обучение с биологическими ограничениями).

1. **Feature Engineering & Latent Space** Модель обрабатывает более 200 входных параметров, включая разреженные категориальные данные (**One-Hot Encoded**). Использование `protobuf==3.20.0` обеспечивает быструю сериализацию данных при обмене между компонентами системы.

2. **Explainable AI (SHAP)** В ветеринарии важен не только результат, но и аргументация. Интеграция SHAP позволяет визуализировать вклад каждого симптома в итоговое решение:
   * *Пример: Модель подсвечивает, что «Живот увеличен» внес +40% к вероятности ГРЖ, а «Нормальная температура» снизила вероятность инфекции на 15%.*

3. **Data Synthesis Protocol** Обучение проводится на массиве данных в **1,500,000 записей**, базирующихся на авторских медицинских профилях, **вручную составленных Ильей Свечниковым**. Это позволяет модели знать редчайшие патологии, с которыми врач встречается раз в 10 лет.

---

## 💻 System Components & UI

Система VetAI разделена на два независимых слоя, взаимодействующих через RESTful API, что позволяет масштабировать вычислительные мощности Backend отдельно от интерфейса.

### 🎨 Frontend (UI Layer)
Пользовательский интерфейс реализован как высоконагруженное веб-приложение для обеспечения кроссплатформенного доступа врачей.

* **Stack:** Streamlit / React-lite
* **Features:**
    * Интерактивный сбор анамнеза с динамической валидацией.
    * Визуализация диагностического вектора в реальном времени.
    * **XAI Dashboard:** Встроенные графики интерпретации (SHAP values) для обоснования диагноза.

<p align="center">
  <img src="https://github.com/user-attachments/assets/a97f57de-9f8b-4a93-9806-1886699ba07e" alt="VetAI Demo UI" width="900">
  <br>
  <i>Интерфейс системы Neural Kernel v15 в режиме диагностической сессии</i>
</p>

### ⚙️ Backend (Inference Engine)
Математическое «сердце» системы, отвечающее за высокопроизводительные вычисления и работу нейросетевого ядра.

* **Stack:** Python 3.9+ / Flask / Gunicorn
* **Architecture:**
    * **Preprocessing Pipeline:** Автоматическая нормализация входных данных и обработка разреженных матриц признаков.
    * **Inference Engine:** Асинхронный запуск моделей TensorFlow с использованием CUDA-ядер.
    * **API Layer:** Стандартизированный интерфейс для обмена данными в формате JSON/Protobuf.
* **Optimization:** Использование многопоточности Gunicorn для обработки параллельных запросов от нескольких клиник.

---

## 📄 Reporting tools

В систему интегрирован модуль генерации ветеринарных протоколов, который позволяет мгновенно превратить результаты анализа в документ, готовый к печати.

### Основные возможности:
* **Автоматическое формирование:** Протокол включает в себя диагноз, расчетную вероятность, полный анамнез и список выявленных симптомов.
* **Поддержка кириллицы:** Использование шрифтов DejaVuSans гарантирует корректное отображение русского текста.
* **Модульная архитектура:** Логика формирования PDF вынесена в отдельный сервис `pdf_generator.py`. Это позволяет:
    * Легко менять дизайн отчета, не затрагивая код нейросети.
    * Добавлять новые поля (результаты анализов, данные тонометрии и т.д.) простым обновлением словаря данных.
    * Масштабировать отчет под разные форматы (A4, мобильный чек и др.).

### Технический стек экспорта:
* **FPDF2:** Библиотека для генерации PDF (выбрана за высокую скорость работы в Python-среде).
* **Byte-streaming:** Документ генерируется "на лету" в оперативной памяти и передается пользователю без создания временных файлов на сервере, что повышает безопасность и производительность.

---

## 🚀 Deployment & Development
### Local Setup via Docker
Для запуска среды разработки со всеми зависимостями:

```bash
# Сборка образа
docker build -t vet-ai-image . --no-cache

# Запуск с поддержкой СPU
docker run -p 8501:8501 --rm -v ${pwd}:/app vet-ai-image python -m streamlit run app.py

# Запуск с поддержкой GPU
docker run --gpus all -p 8501:8501 --rm -v ${pwd}:/app vet-ai-image python -m streamlit run app.py

# Запуск бенчмарка системы
docker run --gpus all --rm -v ${pwd}:/app vet-ai-image python benchmark.py

```

**Download the weight file from the Releases section and place it in the root folder.**

### CI/CD Pipeline
Проект готов к масштабированию через **Kubernetes (K8s)**. Модульная структура `app.py` и использование `joblib` для версионирования весов позволяют проводить **A/B тестирование** различных версий ядра v15.1, v15.2 параллельно.

---

## 📊 Scientific Methodology
В основе **VetAI** лежит научная работа кафедр Росбиотеха и экспертный опыт врачей. Мы трансформируем классические диагностические справочники в математические модели.

* **Precision:** `0.94+` на валидационных сетах.
* **Recall:** `0.91+` (критически важно для исключения ложноотрицательных результатов в экстренной хирургии).
* **Core Inference Latency:** `0.1071ms` Это чистое время, затрачиваемое моделью на выполнение математических операций внутри GPU, актуально для NVIDIA RTX 30-series
* **End-to-End System Latency:** `56ms` Время от момента нажатия кнопки пользователем до вывода результата на экран.

---

## 📊 Performance & Hardware Validation

В рамках технической верификации нейросетевого ядра v15_opt были проведены нагрузочные тесты на различных аппаратных узлах. Результаты демонстрируют прямую зависимость скорости формирования диагностической гипотезы от типа вычислительного устройства.

### Сравнительный анализ латентности инференса
Показатель латентности измерялся как время полного цикла: от передачи вектора признаков в модель до получения вероятностного распределения диагнозов.

| Аппаратная конфигурация | Вычислительный модуль | Core Inference Latency (мс) | End-to-End System Latency (мс) | Статус производительности |
|:---|:---|:---|:---|:---|
| **Workstation:** i5-11400f / 16GB ddr4 3200/ **NVIDIA RTX 3060 12GB GDDR6** | **GPU (CUDA)** | **58 ms** |**0.0695 ms**| **Real-time:** Мгновенный отклик |
| **Workstation:** i5-13400f / 32GB ddr4 3200 / **NVIDIA RTX 4060 8GB GDDR6** | **GPU (CUDA)** | **45 ms** | **0.0423 ms**| **Real-time:** Мгновенный отклик |
| **Mobile Workstation:** r5-3550h / 32GB ddr4 2400 / **NVIDIA GTX 1650 4GB GDDR5** | **GPU (CUDA)** | **93 ms** | **0.0990 ms**| **Real-time:** Мгновенный отклик |
| **Mobile Node:** i7-8650u / 16GB ddr4 2400 (No GPU) | **CPU (AVX2/FMA)** | **276 ms** | **4.4691 ms**| **Stable (Fallback mode):** Допустимая задержка (Input Lag) |
| **Mobile Node:** r7-4700u / 16GB ddr4 3200 (No GPU) | **CPU (AVX2/FMA)** | **238 ms** | **4.2431 ms**| **Stable (Fallback mode):** Допустимая задержка (Input Lag) |
| **Mobile Node:** i5-2520M / 4GB ddr3 1333 (No GPU) | **CPU (AVX)** | **1467 ms** | **25.8865 ms**| **Not stable (Fallback mode):** Критическая задержка (Input Lag) |

### Выводы по оптимизации:
* **Адаптивность инференса:** Система сохраняет полную функциональность при отсутствии дискретного графического ускорителя. Переключение на CPU-вычисления (Fallback mode) обеспечивает стабильную работу на мобильных устройствах, хотя и сопровождается возрастанием латентности, обусловленным последовательной обработкой тензоров.
* **Аппаратные требования:** Для развертывания системы в условиях клиник с высокой интенсивностью приема рекомендуется использование аппаратных решений с поддержкой **NVIDIA CUDA**, что снижает время ожидания для конечного пользователя более чем в 20 раз.
* **Эффективность архитектуры:** Даже в CPU-режиме время обработки запроса остается в рамках физиологических норм восприятия пользователя для аналитических систем, не требуя длительного ожидания завершения процесса вычислений.
* **Аппаратная оптимизация и кроссплатформенность:** Архитектура системы спроектирована с учетом гетерогенности современных вычислительных сред. Программный комплекс демонстрирует высокую стабильность и конкурентную латентность при работе на различных микроархитектурах.
* **x86_64 Optimization (Intel/AMD):** Инференс на центральных процессорах оптимизирован за счет использования векторных инструкций AVX2/FMA. Система обеспечивает стабильный отклик как на решениях от Intel, так и на AMD Ryzen, что позволяет развертывать комплекс на стандартном офисном оборудовании без специализированных ускорителей.
* **NVIDIA CUDA Acceleration:** Максимальная производительность и минимальный уровень джиттера (колебаний времени отклика) достигаются при использовании графических процессоров NVIDIA. **Серия GTX 16 и выше:** Обеспечивает качественный скачок производительности, переводя обработку данных в параллельный режим CUDA-ядер. **Серия RTX 30 и выше:** Позволяет задействовать специализированные тензорные ядра (Tensor Cores) и алгоритмы смешанной точности (Mixed Precision), что выводит латентность на уровень мгновенного аппаратного отклика, необходимого для высоконагруженных серверных узлов.
---

## 🛠 Engineering & Performance Optimization

В версии v15.1 был проведен комплекс мероприятий по глубокой оптимизации вычислительного конвейера, направленный на минимизацию задержек (latency) и повышение отказоустойчивости системы.

### 1. Вычислительная оптимизация (Mixed Precision)

Для эффективного использования аппаратных ресурсов GPU (архитектуры NVIDIA Ampere и Turing) внедрена технология смешанной точности (Mixed Precision).

* **Механизм:** Большинство тензорных операций переведено в формат float16, в то время как критические узлы (финальный слой Softmax) сохранены в float32 для обеспечения численной стабильности.

* **Результат:** Активация ядер Tensor Cores позволила достичь удельной производительности в 0.107 ms на одну операцию при пакетной обработке (Batch Inference), что делает систему пригодной для обработки Big Data.

### 2. Оптимизация среды исполнения (Runtime Efficiency)

Проведено исследование влияния среды виртуализации на производительность инференса.

* **Native Linux vs WSL2:** Установлено, что отсутствие абстракций гипервизора в нативной среде Linux сокращает задержку обработки на CPU в 2.4 раза (с 255 ms до 106 ms).

* **Instruction Set Adaptation:** Алгоритм оптимизирован под наборы инструкций AVX, что гарантирует стабильную работу на Legacy-оборудовании (протестировано на архитектуре Intel Sandy Bridge) без поддержки современных расширений AVX2/AVX-512.

### 3. Рефакторинг сериализации и CI/CD

Для устранения несовместимости механизмов LossScaleOptimizer и формата HDF5 в современных версиях TensorFlow, была изменена логика сохранения артефактов.

* **Persistence Strategy:** Реализована схема экспорта весов с исключением графа оптимизатора (include_optimizer=False).

* **Контейнеризация:** Конфигурация Docker-образа унифицирована для бесшовного проброса GPU-интерфейсов через NVIDIA Container Toolkit, обеспечивая автоматическое согласование CUDA-драйверов хоста и гостевой ОС.

---

## 🖥️ System Requirements Specification

Программный комплекс VetAI v0.4.0 оптимизирован для архитектуры **x86_64** и поддерживает гетерогенные вычисления (автоматическое переключение GPU ↔ CPU).

### 1. Минимальные требования (Minimum)
*Для работы в режиме обратной совместимости или базового использования.*

| Компонент | Локальная станция (Client) | Серверный узел (Standard Server) |
| :--- | :--- | :--- |
| **Процессор (CPU)** | **Intel Core i3-10100** / **Ryzen 3 3100** | **Intel Core i5-11400** / **Ryzen 5 3600** |
| **Оперативная память** | 16 GB DDR4 | 16 GB DDR4/DDR5 |
| **Видеокарта (GPU)** | Интегрированная (AVX2 support) | **NVIDIA GTX 1650** / **GTX 1660** |
| **Видеопамять (VRAM)** | — | 4 GB GDDR5 |
| **Latency (отклик)** | ~200 - 300 ms | ~30 - 50 ms |

### 2. Рекомендуемые требования (Recommended)
*Для обеспечения мгновенного отклика и многопоточной обработки.*

| Компонент | Локальная станция (High-end) | Серверный узел (Enterprise) |
| :--- | :--- | :--- |
| **Процессор (CPU)** | **Intel Core i5-12400** / **Ryzen 5 5600H** | **Intel Core i7-12700** / **Ryzen 7 5800X** |
| **Оперативная память** | **32 GB DDR4/DDR5** | **32 GB+ DDR5** |
| **Видеокарта (GPU)** | **NVIDIA RTX 3060** / **4060 Mobile** | **NVIDIA RTX 4070** / **RTX 3080** |
| **Видеопамять (VRAM)** | 6 - 8 GB | **12 GB+ GDDR6X** |
| **Latency (отклик)** | ~30 - 45 ms | **< 10 ms** |

### 🛠 Программная среда
* **ОС Windows:** 10/11 (64-bit) + **WSL2** (Windows Subsystem for Linux).
* **ОС Linux:** Ubuntu 20.04 LTS / 22.04 LTS (рекомендуется).
* **Драйверы:** NVIDIA Game Ready / Studio Driver с поддержкой **CUDA 11.8+** и **cuDNN**.
* **Архитектура:** x86_64 (обязательна поддержка инструкций AVX2 процессором).

---

## 📬 Contacts & Scale-up
VetAI активно ищет партнерства с ветеринарными клиниками для пилотного внедрения.

* **University Supervisor:** [Rosbiotech Team]
* **Version:** 0.4.0 Alpha (Not for clinical use yet)
